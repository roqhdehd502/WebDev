package fourthWeek;

public class INum {
	private int num;
	public INum(int num) {
		this.num = num;
	}
	
	/*
	@Override
	public boolean equals(Object obj) {
		if (this.num == ((INum)obj).num) { // 부모 -> 자식 객체 타입으로 캐스팅
			return true;
		} else {
			return false;
		}
	}
	*/
}
